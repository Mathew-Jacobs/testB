@model Portlets.MVC.Models.BookList

@{
    ViewBag.Title = "BookList";
}

<h2>BookList</h2>

@foreach (var book in Model.Books)
{
    <h3>
        @book.title
    </h3>
    <h4>
        @book.isbn
    </h4>
    <hr />
}